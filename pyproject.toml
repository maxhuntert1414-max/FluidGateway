[project]
name = "fluidgateway"
version = "0.64.0"
description = "An evidence-driven gateway for finding waste across CPU, GPU, memory, and frame pipelines."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [
  { name = "FluidGateway contributors" }
]
keywords = ["presentmon", "gpu", "cpu", "ram", "vram", "runtime", "performance", "frame-pacing"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Topic :: Software Development :: Testing",
  "Topic :: System :: Benchmark"
]

[project.scripts]
fluidgateway = "fluidgateway.cli:main"
