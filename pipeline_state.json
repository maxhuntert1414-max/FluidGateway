{
  "pipeline": "STARK-PRODUCTIONS",
  "fallback_mode": false,
  "current_stage": "v0.50-daemon-action-execution",
  "active_roles": [
    "RESEARCHER",
    "PLANNER",
    "CODER",
    "REVIEWER",
    "MANAGER",
    "MEMORY"
  ],
  "inactive_roles": {
    "DESIGNER": "No UI or visual asset scope in this slice.",
    "TRANSFORMER": "No asset or format conversion scope in this slice.",
    "DEBUGGER": "Reviewer issue was fixed directly by CODER; no separate debugger agent was needed."
  },
  "blockers": [],
  "review_status": "locally-verified",
  "decisions": [
    {
      "id": "v0.46-scope",
      "decision": "Add a local runtime daemon dry-run loop around adapter replays.",
      "rationale": "This moves FluidGateway from isolated adapter sessions toward a persistent advisory manager loop while preserving the no-mutation boundary."
    },
    {
      "id": "v0.46-state-contract",
      "decision": "Require --state for run-daemon and normalize it before both load and write.",
      "rationale": "The daemon must have an explicit persistence boundary and extensionless state paths must not ignore existing or invalid JSON state."
    },
    {
      "id": "v0.46-path-safety",
      "decision": "Reject daemon --state and --out when they resolve to the same JSON path.",
      "rationale": "State and report writes have different schemas and must not overwrite each other."
    },
    {
      "id": "v0.47-host-capability-snapshot",
      "decision": "Attach a read-only host capability snapshot to daemon dry-run reports.",
      "rationale": "A future intelligent manager needs CPU, RAM, GPU, and VRAM context before it can make host-aware scheduling or memory-residency decisions."
    },
    {
      "id": "v0.48-daemon-decision-plan",
      "decision": "Translate host and supervisor evidence into a dry-run daemon decision plan.",
      "rationale": "The project needs a manager-shaped intent layer before any native scheduler or RAM/VRAM residency action can be considered."
    },
    {
      "id": "v0.49-daemon-action-queue",
      "decision": "Map daemon decisions into backend-shaped dry-run action queue entries.",
      "rationale": "A future native manager needs concrete CPU/GPU/RAM/VRAM action envelopes with safety status, backend requirements, and expected signals."
    },
    {
      "id": "v0.50-daemon-action-execution",
      "decision": "Execute queued daemon actions only through an advisory/read-only dry-run executor.",
      "rationale": "The manager loop needs an execution contract now, while native scheduler, memory, GPU, VRAM, and privileged actions must remain blocked until a real backend and safety review exist."
    }
  ],
  "quality_gates": [
    "python -m unittest",
    "python -m compileall fluidgateway tests",
    "python -m fluidgateway --version",
    "git diff --check",
    "runtime daemon two-stream smoke",
    "runtime daemon extensionless state smoke",
    "runtime daemon host snapshot smoke",
    "runtime daemon decision plan smoke",
    "runtime daemon action queue smoke",
    "runtime daemon action execution smoke",
    "git ls-remote origin refs/heads/main"
  ]
}
